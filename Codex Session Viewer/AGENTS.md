# Agent Notes

- User requests a macOS SwiftUI app that visualizes Codex CLI sessions stored under `~/.codex/session` by month as `.jsonl`.
- After finishing each project part, perform a `git commit`; commit messages should follow the style `feat(scope): description` (example: `feat(ui): update click button`).
- Maintain ASCII files unless a file already uses other character sets.
